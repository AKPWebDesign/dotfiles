# check if we have internet. if not, we'll set an environment variable that we can check later.
if ! curl -sf -m 2 https://www.google.com/generate_204 >/dev/null 2>&1; then
  export NO_INTERNET=true
fi